name: local_skill_share
description: A new Flutter project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Moved to dev_dependencies (standard practice for tools)
  flutter_riverpod: ^2.5.1
  dio: ^5.4.0
  firebase_core: ^3.15.2
  firebase_auth: ^5.7.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  go_router: ^14.0.0
  geolocator: ^12.0.0
  google_maps_flutter: ^2.6.0
  razorpay_flutter: ^1.3.5
  flutter_stripe: ^11.0.0
  uuid: ^4.3.3
  intl: ^0.19.0
  flutter_dotenv: ^5.0.2
  image_picker: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  flutter_launcher_icons: "^0.14.4" # ✅ Add here

# ✅ NEW CONFIGURATION BLOCK
flutter_launcher_icons:
  android: "launcher_icon" # ✅ Forces name to be 'launcher_icon'
  ios: true
  image_path: "assets/icon/icon.png" # ✅ Path to your source icon image
  adaptive_icon_background: "#FFFFFF" # Recommended for modern Android icons
  adaptive_icon_foreground: "assets/icon/icon.png"

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/icon/ # ✅ Ensure your icon is included in assets